services:

  broker:
    image: apache/kafka:latest
    hostname: broker
    container_name: broker
    mem_limit: 256m
    cpu_count: 1
    healthcheck:
      test: [ "CMD", "/opt/kafka/bin/kafka-cluster.sh", "cluster-id", "--bootstrap-server", "localhost:9092" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    ports:
      - "9092:9092"
      - "9101:9101"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_ADVERTISED_LISTENERS: 'PLAINTEXT://broker:29092,PLAINTEXT_HOST://localhost:9092'
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_PROCESS_ROLES: 'broker,controller'
      KAFKA_CONTROLLER_QUORUM_VOTERS: '1@broker:29093'
      KAFKA_LISTENERS: 'PLAINTEXT://broker:29092,CONTROLLER://broker:29093,PLAINTEXT_HOST://0.0.0.0:9092'
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: 'PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT,CONTROLLER:PLAINTEXT'
      KAFKA_INTER_BROKER_LISTENER_NAME: 'PLAINTEXT'
      KAFKA_CONTROLLER_LISTENER_NAMES: 'CONTROLLER'
      KAFKA_LOG_DIRS: '/tmp/kraft-combined-logs'
      KAFKA_HEAP_OPTS: '-Xms128m -Xmx128m'
      KAFKA_MESSAGE_MAX_BYTES: 10485760
      KAFKA_REPLICA_FETCH_MAX_BYTES: 10485760
      KAFKA_NUM_NETWORK_THREADS: 1
      KAFKA_NUM_IO_THREADS: 1
      KAFKA_BACKGROUND_THREADS: 1
      KAFKA_SOCKET_REQUEST_MAX_BYTES: 10485760
      KAFKA_DEFAULT_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_NUM_PARTITIONS: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_LOG_CLEANER_ENABLE: 'false'
      KAFKA_LOG_CLEANER_DEDUPE_BUFFER_SIZE: '16777216'

    networks:
      - core-infra

  generator:
    image: udhayarajan/core-infra-generator:latest
    build:
      context: .
      dockerfile: generator/Dockerfile
    container_name: generator
    mem_limit: 128m
    cpu_count: 1
    networks:
      - core-infra
    depends_on:
      broker:
        condition: service_healthy
    environment:
      "KAFKA_BROKER": "broker:29092"
    command: [ "-max=50_000_000", "-workers=1" ] # it's event count


  processor:
    image: udhayarajan/core-infra-processor:latest
    mem_limit: 1536m
    cpu_count: 1
    build:
      context: .
      dockerfile: processor/Dockerfile
    container_name: processor
    networks:
      - core-infra
    depends_on:
      broker:
        condition: service_healthy
    environment:
      "KAFKA_BROKER": "broker:29092"
    command: [ "-max=50_000_000", "-batch=80_000_000" ] # it's batch size in bytes (is equal to 80MB)

  validator:
    image: udhayarajan/core-infra-validator:latest
    cpu_count: 1
    mem_limit: 128m
    build:
      context: .
      dockerfile: validator/Dockerfile
    container_name: validator
    volumes:
      - ./csv:/csv
      - ./results:/results
    networks:
      - core-infra
    depends_on:
      broker:
        condition: service_healthy
    environment:
      "KAFKA_BROKER": "broker:29092"

networks:
  core-infra:
    driver: bridge
